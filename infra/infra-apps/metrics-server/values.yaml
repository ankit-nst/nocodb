matrics-server:
  replicas: 2
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  podDisruptionBudget:
    enabled: true
    maxUnavailable: 1
  metrics:
    enabled: true
  serviceMonitor:
    enabled: false
